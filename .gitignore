__pycache__/
*.pyc
*.egg-info/
build/
dist/
.venv/
# 24MB full-quality recording lives as a GitHub release asset, not in git
demo/5-agent-blocked.gif

# design doc grows into its own project — stays out of this repo
docs/design/
device-connect-vs-*.md

# private strategy/business docs — never leave this machine
/private/

# cross-session audit state (stage-gate workflow) — local coordination only
/audit/

# ZCode workspace discipline file — local tool config, never ships
/AGENTS.md

# ZCode workspace-scoped agent definitions — local tool config, never ships
/.zcode/
